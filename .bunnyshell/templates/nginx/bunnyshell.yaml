kind: Environment
name: Fooenv
type: primary
components:
    - kind: Application
      name: nginx
      dockerCompose:
          image: nginx:latest
          ports:
              - '80:80'
      hosts:
          -
              hostname: 'app-{{ env.base_domain }}'
              path: /
              servicePort: 80

